// EduLog - shared frontend behaviour.
// Page-specific scripts live alongside this file and are loaded per template.

document.addEventListener('DOMContentLoaded', () => {
  console.debug('EduLog ready');
});
